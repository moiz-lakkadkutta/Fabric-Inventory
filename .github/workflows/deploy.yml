# Stub — to be filled in TASK-067 (Hetzner deploy).
#
# Triggers on semver tags (v1.0.0, v0.2.1, ...). Real impl will:
#   - build a multi-stage Docker image (backend + frontend bundle),
#   - push to GHCR or Hetzner Container Registry,
#   - SSH to the Hetzner CX22 box and `docker compose pull && up -d`,
#   - run Alembic migrations as a one-shot job before the API restarts.
#
# Until then, every job here is an `echo` placeholder so the workflow
# parses cleanly and shows up in the Actions tab.

name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  build-image:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TASK-067 — build multi-stage image (backend + frontend)."

  push-to-registry:
    name: Push to registry
    runs-on: ubuntu-latest
    needs: build-image
    steps:
      - name: Placeholder
        run: echo "TASK-067 — push image to GHCR or Hetzner registry."

  deploy-to-hetzner:
    name: Deploy to Hetzner
    runs-on: ubuntu-latest
    needs: push-to-registry
    steps:
      - name: Placeholder
        run: echo "TASK-067 — SSH to Hetzner CX22, pull image, run migrations, restart stack."
