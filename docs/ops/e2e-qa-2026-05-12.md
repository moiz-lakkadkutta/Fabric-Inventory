# E2E manual QA — 2026-05-12

Run on `main` @ `81f3878`, fresh signup `e2e-test-0512@example.com`, firm `E2E Textiles Mumbai`.

## Coverage
| # | Area | Result |
|---|---|---|
| 1 | Onboarding / signup | Pass (B1) |
| 2 | Auth (login, logout, forgot + rate-limit) | Pass (B2) |
| 3 | Masters (Parties, Items, SKUs, UOM/HSN) | Pass (B3, B4) |
| 4 | Sales (Invoice DRAFT→FINALIZED→PAID + PDF) | Pass with workaround (B5, B6, B7) |
| 5 | Procurement (PO → GRN → PI) | Pass (B8, B9, B10, B11, B12) |
| 6 | Sales Order + Delivery Challan | Pass with math bug (B13) |
| 7 | Inventory + Adjust Stock | Pass with known SOH lag (B14) |
| 8 | Accounting (Receipts, Vouchers, Banks, Cheques) | **FAIL** (B15, B16, B19) |
| 9 | Reports (P&L, TB, GSTR-1, Stock, Daybook) | Partial (B17, B18) |
| 10 | Exports CSV / Excel | Mostly pass (B20) |
| 11 | Job-work (send-out, receive-back, karigar cards) | Pass with UX gaps (B21, B22, B23) |
| 12 | Admin (users, invite, role swap, last-owner) | Pass (B24) |

## Bugs filed

### P0 — books-corrupting
- **B19** — Bank-account opening balance posts unbalanced voucher; TB rejects every subsequent call with `DR=11050 vs CR=1050`. Filed as **#97**.

### High — feature-blocking
- **B7** — `/invoices/{id}/pdf` 500s when uvicorn runs without `DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib` (WeasyPrint can't find libgobject). Suggest startup fail-fast.
- **B13** — Sales Order rate input stored 10,000× too small (₹500 entered → ₹0.01 saved, ₹0.20 line total). Carries through to DC.
- **B16** — Purchase Invoice POST does not create an accounting voucher. PI of ₹35,000 + ₹1,750 GST never lands in GL → no Purchases expense, no Input GST credit, no AP entry.

### Medium
- **B3** — Form submit errors show generic "Validation error" without surfacing BE's `field_errors` map. Found in NewPartyDialog + NewItemDialog.
- **B4** — NewPartyDialog never calls `idem.reset()` on close or error → retry after a 422 fails with `IDEMPOTENCY_KEY_PAYLOAD_MISMATCH` until full page reload.
- **B5** — InvoiceList missing Excel export button (CSV-only).
- **B9** — PO/GRN/PI detail/line rows render the item as raw UUID prefix `81bc3ff5…` instead of "Cotton Suit". Sales Order + DC + Items dropdowns get this right.
- **B14** — `/inventory` SOH and active-lot counts both show 0 after GRN+DC posted (already documented as Wave-5 carry-over; workaround `/reports/stock-summary`).
- **B17** — `/reports` Trial Balance tab shows ₹0/₹0 in the UI even though balanced vouchers exist — likely period or firm filter mis-applied on FE.
- **B18** — `/reports` Stock tab on-hand value correct (85) but Rate=₹0 and Value=₹0; stock valuation not reading the posted PI cost.
- **B20** — `GET /purchase-orders?format=csv` ignores the format param and returns JSON. Other list endpoints (`/parties`, `/items`, `/invoices`, `/bank-accounts`, `/vouchers`, `/receipts`) respect it correctly.
- **B22** — Job-work Active jobs table shows SENT qty as `0` for a 10-piece send-out. Display formatter bug — qty is stored fine (challan 0001 reachable).
- **B23** — `Receive back` dialog only exposes Date + Notes inputs. Validator demands "Enter a finished or wastage qty on at least one line" but the per-line qty/wastage inputs are not visible in the default state. Either reveal them or fix the validator copy.

### Low / polish
- **B1** — `/onboarding` Vyapar adapter card still says "coming soon" — Vyapar is the primary adapter per CLAUDE.md.
- **B2** — `/forgot` page pre-fills `rajesh@example.com` (legacy dev seed). Test-account branding leakage.
- **B6** — `/sales/invoices/new` defaults `issue_date` to **30 Apr** (last day of last month) instead of today.
- **B8** — Side nav under Purchase exposes only the PO list — no GRN/PI links until you're on a child route. Sales sidebar exposes all 5 children correctly.
- **B10** — PO post-approve flips status label to `OPEN` instead of `APPROVED` (state name vs label drift).
- **B11** — `PurchaseOrderDetail` page renders raw `VALIDATION_ERROR` envelope when the `:id` route param isn't a UUID (e.g., typo `/purchase/pis`). Should 404 instead.
- **B12** — GRN status reads `RECEIVED` on its detail page but `ACKNOWLEDGED` in the PI source-GRN dropdown. Pick one.
- **B15** — Receipt and voucher narrations show `Receipt from party 4c0635f6-…` / `Sale to party 4c0635f6-…`. Should resolve to party name.
- **B21** — Job-work `Send out` dialog defaults the UOM textbox to `METER` regardless of the chosen item's primary UOM. Should pre-fill from the item.
- **B24** — Invite-user dialog footer text says "Email delivery wires up in Wave 5" — Wave 5 + 6 are now done.

## Test data left behind in dev DB
- User `e2e-test-0512@example.com` / `TestPass2026!` / Org `E2E Test Co 0512` / Firm `8f5a605c-556d-413b-a2d1-752e9c61b157`
- Parties: ACME Saree Centre, Surat Silk Mills, Imran Embroidery Works (1 karigar)
- Items: Cotton Suit (`81bc3ff5-…`, HSN 6204, GST 5%, PIECE)
- Transactions: RT/2526/0001 PAID, PO/25-26/0001 CONFIRMED, GRN/25-26/0001 RECEIVED, PI/25-26/0001 POSTED, SO/2526/0001 CONFIRMED, DC/2526/0001 ISSUED, RCT/2526/0001 POSTED, jobwork challan 0001 SENT
- Bank: HDFC Mumbai Branch (₹10,000 opening — this is the one that broke TB, see B19)
