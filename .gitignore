# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node
node_modules/
dist/
build/
coverage/
playwright-report/
test-results/

# Env / secrets
.env
.env.local
.env.*.local
ops/.env.backup

# Backup artefacts (TASK-CUT-404). Never commit dumps; .gpg or otherwise.
ops/backups/

# Vyapar real-backup samples (TASK-TR-D3-PREP). Never commit — even sanitized
# files belong out of git; the spike runner only needs them locally. See
# docs/spikes/vyapar-real-backup-protocol.md.
docs/spikes/vyapar-sample-export*.xlsx
docs/spikes/vyapar-sample-export-raw*.xlsx

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code — user-local settings only; project commands in .claude/commands/ are shared
.claude/settings.local.json
.claude/worktrees/

# Build artifacts
*.log
